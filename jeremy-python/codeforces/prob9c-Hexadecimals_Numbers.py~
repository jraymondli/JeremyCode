import array
number = int(input())
binaryList = array.array("i", (str(i) for i in (range(number, 0, -1))))
binaryList = [str(i) for i in binaryList]
count = len([s for s in binaryList if not s.strip('10')])
print count



'''
for y in range(len(falseNum)):
        if str(falseNum[y]) not in str(start):
            ticker =  ticker + 1
    if ticker == 8:
        count = count + 1
    ticker = 0
    start = start + 1
'''
